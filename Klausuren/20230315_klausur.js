// THEMEN:
// Ein lauffäiges Projekt wird über Github ausgeteilgt.
// Das Projekt enthält keine Kommentare.
// Mögliche Aufgabe:
// Einzelne Zeilen mit Kommentaren beschreiben
// Also: was passiert in der Zeile...
// Es soll eine neue Seite angelegt werden mit einem Formular. meineApp.get und post einbauen 
// Beim Klick auf Button sollen Werte eingelesen werden und
// ggfs. auch in die Datenbenk geschrieben werden.
// Es soll auch etwas mit if und else (auch verschachtelt) programmiert werden
// MeineApp.get anpassen usw.
// Wenn ein bestimmter button geglcikt wird bzw. eine bestimmte seite aufgerufen wird,
// dann kommt es zur eine Fehlermeldung. Der Fehler muss korrigiert werden.
// Datentyp Int bei laufenden nummern, float oder double (nachkommastellen) währungsbeträge, bei varchar vorname usw
// dbVerbindung angucken
// eine neue Tabelle nach Vorgabe in der Datenbank anlegen
//in ejs nur beschrieben, um etwas ausführen < > setzen